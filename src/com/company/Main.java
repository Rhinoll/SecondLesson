package com.company;

import java.util.ArrayList;
import java.util.Scanner;

public class Main {

    public static void main(String[] args) {
        /*
        int x = 0;
        while (x > 0 || x != 3 || x == 5) {
            //здесь свой код
        }

        for (int i = 0; i < 10; i++) {
            if (i == 5) {
                continue;
                break; //выход из цикла
            }
            //здесь дальше свой код
        }


        int[] a = new int[5]; //массивы 0 0 0 0 0
        int b[] = new int[10];// нежелательный вариант
        //заполнение происходит только после инициализации
        for (int i = 0; i < b.length; i++) { //обычный цикл
            System.out.print(b[i] + " ");
        }
        for (int i : b) System.out.println(i + " "); //цикл foreach

        int[] c = {6, 4, 2, 900, -9333};
        c[2] = 45; //тоже вариант заполнения

        //двумерный массив
        int [][] y = new int [4][4];//первое число - строки, вторая - стобцы
        for (int i = 0; i < y[i].length;i++) {
            for (int j = 0; j < y[j].length; j++) {
                System.out.print(y[i][j] + " ");
            }
            System.out.println();
        ArrayList aL = new ArrayList<>(); //20 элементов по умолчанию. Саморасширяющий массив;
        aL.add(0);
        aL.add("qwerty");
        int v = (int) aL.get(2); //невозможно вытащить строку в виде int

        ArrayList<Integer> list = new ArrayList<>();
        list.add(123);
        //list.add("qwerty"); метод защиты от случайного попадания

        int g = 0;
        int h;
        if (g == 100) {
            h = 900;
        }
        g = 1000;


        Scanner scan = new Scanner(System.in);
        for (int i = 0; i < 10; i++) {
            b[i] = scan.nextInt();
            }
            for (int i: b)
             */


        }
    }





